{
  "name": "http-server",
  "version": "1.0.0",
  "description": "Simple HTTP server for serving HTML pages",
  "main": "index.js",
  "scripts": {
    "start": "node index.js --port 3000"
  },
  "dependencies": {
    "minimist": "^1.2.8"
  }
}
